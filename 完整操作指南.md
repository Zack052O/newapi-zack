# New-API 自定义侧边栏 — 完整操作指南

## 你的问题解答

---

### Q1：Fork 了之后怎么改源代码？是直接部署改好的源码吗？

**是的，直接用改好的源码部署。** 你不需要再手动改任何代码——我已经帮你改好了。

T7 硬盘 `/Volumes/T7/VUE/newapi` 里的就是完整改好的源码，可以直接用来构建 Docker 镜像。

#### 操作步骤：

```bash
# 1. 在 GitHub 上 Fork QuantumNous/new-api 到你自己的账号
#    （如果你还没 fork 的话）

# 2. 把 T7 上的改好源码推送到你的 fork
cd /Volumes/T7/VUE/newapi

# 查看当前远程仓库
git remote -v
# 输出：origin  https://github.com/QuantumNous/new-api.git

# 把 origin 改成你的 fork（替换 YOUR_USERNAME）
git remote set-url origin https://github.com/YOUR_USERNAME/new-api.git

# 添加上游仓库（用于后续同步更新）
git remote add upstream https://github.com/QuantumNous/new-api.git

# 提交我们的改动
git add -A
git commit -m "feat: add custom sidebar links configuration"

# 推送到你的 fork
git push origin main
```

#### 部署到服务器：

```bash
# 方式一：在服务器上 clone 你的 fork 然后 Docker 构建
ssh your-server
git clone https://github.com/YOUR_USERNAME/new-api.git
cd new-api
docker build -t new-api-custom .
docker stop new-api && docker rm new-api
docker run -d --name new-api \
  -p 3000:3000 \
  -v /path/to/data:/data \
  -e SQL_DSN="你的数据库连接" \
  new-api-custom

# 方式二：如果你用 docker-compose
# 修改 docker-compose.yml 中 image 字段，或用 build 字段
```

---

### Q2：后续怎么更新（同步上游新功能）？

你的 fork 需要定期从上游（QuantumNous/new-api）拉取新功能。操作很简单：

```bash
cd /Volumes/T7/VUE/newapi

# 1. 拉取上游最新代码
git fetch upstream

# 2. 合并上游的 main 分支到你的代码
git merge upstream/main

# 3. 如果有冲突，手动解决冲突后：
git add -A
git commit -m "merge: sync upstream"

# 4. 推送到你的 fork
git push origin main

# 5. 在服务器上重新构建部署
```

#### 冲突会有吗？

**大概率不会有冲突**，因为：
- 我们的改动集中在**侧边栏配置**这一块，不涉及核心 API 逻辑
- 新增的文件（`custom-sidebar-links-section.tsx`）完全是新文件
- 修改的文件只改了几行，都是追加性质的

如果上游也改了同一个文件（比如 `use-sidebar-data.ts`），可能会有小冲突，但很容易解决。

#### 也可以用 GitHub 网页操作：

1. 打开你 fork 的 GitHub 页面
2. 点击 "Sync fork" 按钮
3. 如果有冲突，GitHub 会提示你用网页编辑器解决

---

### Q3：数据库可以用同一套吗？

**可以，完全没问题！** 而且不需要做任何数据库迁移。

#### 原因：

1. **没有改数据库结构**：new-api 的 `options` 表是一个 key-value 键值表（`key` 字段 + `value` 字段），我们的 `CustomSidebarLinks` 就是往这个表里加一行新数据，不创建新表、不改字段

2. **向后兼容**：当你用新镜像替换旧镜像时：
   - 启动时读取 `options` 表，发现没有 `CustomSidebarLinks` 这个 key → 自动用默认值（空字符串 = 没有自定义链接）
   - 你在管理后台保存自定义链接后 → 自动创建这条记录
   - 即使你以后换回原版镜像，这条多余的数据也不会造成任何问题

3. **同服务器同数据库**：只要 Docker 容器连接的是同一个数据库（同一个 `SQL_DSN`），数据完全共享

#### 具体操作：

如果你现在的 new-api 是这样跑的：
```yaml
# docker-compose.yml（你现在的配置）
services:
  new-api:
    image: quantumnous/new-api:latest
    environment:
      - SQL_DSN=root:password@tcp(mysql:3306)/new-api
    volumes:
      - ./data:/data
```

你只需要把 image 换成你自己构建的镜像：
```yaml
services:
  new-api:
    # 改成你自己构建的镜像名
    image: new-api-custom:latest
    # 或者用 build 从源码构建
    # build: /path/to/newapi-source
    environment:
      - SQL_DSN=root:password@tcp(mysql:3306)/new-api  # ← 完全相同的数据库！
    volumes:
      - ./data:/data    # ← 完全相同的数据目录！
```

然后：
```bash
docker-compose up -d --build
```

**旧数据完全保留，用户、渠道、令牌、日志全都不变，只是多了一个自定义侧边栏的功能。**

---

### Q4：完整的部署流程（从零开始）

#### 前提条件
- 你现在已有一台服务器，上面跑着 new-api（Docker 方式）
- 你有这台服务器的 SSH 访问权限

#### 步骤 1：推送代码到 GitHub

```bash
cd /Volumes/T7/VUE/newapi

# 设置远程仓库为你自己的 fork
git remote set-url origin https://github.com/YOUR_GITHUB_USERNAME/new-api.git
git remote add upstream https://github.com/QuantumNous/new-api.git

# 提交并推送
git add -A
git commit -m "feat: add custom sidebar links"
git push -u origin main
```

#### 步骤 2：在服务器上构建

```bash
ssh your-server

# 备份当前容器配置（以防万一）
docker ps > new-api-backup.txt
cp docker-compose.yml docker-compose.yml.bak

# 拉取你改好的代码
git clone https://github.com/YOUR_GITHUB_USERNAME/new-api.git
cd new-api

# 构建 Docker 镜像
docker build -t new-api-custom .

# 停止旧容器，用新镜像启动
docker stop new-api
docker rm new-api

# 启动新容器（用完全相同的数据库和端口）
docker run -d \
  --name new-api \
  -p 3000:3000 \
  -v /path/to/your/data:/data \
  -e SQL_DSN="你原来一模一样的数据库连接" \
  -e REDIS_CONN_STRING="你原来一模一样的 Redis 连接（如果有的话）" \
  new-api-custom
```

#### 步骤 3：验证

1. 打开 `https://newapi.zackary.cn`，确认网站正常
2. 登录管理员账号
3. 进入 系统设置 → 站点 → 自定义侧边栏链接
4. 添加一个测试链接，保存，确认侧边栏出现

---

### 文件清单

T7 硬盘 `/Volumes/T7/VUE/newapi/` 目录结构：

```
/Volumes/T7/VUE/newapi/
├── 完整操作指南.md          ← 你正在看的这个文件
├── 部署指南.md              ← 精简版部署步骤
├── 功能预览.md              ← 侧边栏效果预览
├── custom-sidebar-links.patch ← 纯补丁文件（备用）
├── Dockerfile               ← Docker 构建文件
├── docker-compose.yml       ← Docker Compose 配置
├── main.go                  ← Go 后端入口
├── controller/              ← 后端控制器（已修改 misc.go）
├── model/                   ← 后端模型（已修改 option.go）
├── web/                     ← 前端源码（已修改 8 个文件）
│   └── src/
│       ├── hooks/use-sidebar-data.ts           ← 侧边栏数据（已改）
│       ├── components/layout/components/
│       │   └── nav-group.tsx                    ← 导航组件（已改）
│       └── features/system-settings/
│           └── maintenance/
│               ├── config.ts                     ← 配置类型（已改）
│               └── custom-sidebar-links-section.tsx ← 新增！管理UI
└── ...
```

---

### 总结

| 问题 | 答案 |
|------|------|
| 怎么改源代码？ | 已经改好了，T7 里的就是完整源码 |
| 怎么部署？ | 推送到 GitHub → 服务器上 Docker 构建部署 |
| 怎么更新？ | `git fetch upstream && git merge upstream/main` |
| 数据库能用同一套吗？ | **能**，不需要任何数据库改动 |
