# New-API 自定义侧边栏链接功能 — 部署指南

## 功能概述

本补丁为 QuantumNous/new-api 添加了**自定义侧边栏链接**功能，管理员可以在系统设置页面动态添加、编辑、排序和删除侧边栏链接，无需修改代码或重新编译。

### 特性

- ✅ 在管理后台 **系统设置 → 站点 → 自定义侧边栏链接** 中管理链接
- ✅ 支持**内部链接**（如 `/pricing`）和**外部链接**（如 `https://docs.example.com`）
- ✅ 外部链接自动在新标签页打开，并显示外部链接图标
- ✅ 支持**自定义图标**（从 26 种 lucide 图标中选择）
- ✅ 支持**徽章文字**（如 "New"、"Beta"）
- ✅ 支持**角色权限控制**（访客/登录用户/管理员/超级管理员）
- ✅ 支持**拖拽排序**
- ✅ 配置通过后端 API 存储，即时生效，无需重启

---

## 修改的文件清单

### 后端（Go）

| 文件 | 修改内容 |
|------|----------|
| `controller/misc.go` | 在 `GetStatus` API 响应中添加 `CustomSidebarLinks` 字段 |
| `model/option.go` | 在 `InitOptionMap` 中注册默认值；添加 `validateCustomSidebarLinks` 验证函数 |

### 前端（React/TypeScript）

| 文件 | 修改内容 |
|------|----------|
| `web/src/features/system-settings/maintenance/config.ts` | 添加 `CustomSidebarLink` 类型定义和解析/序列化函数 |
| `web/src/features/system-settings/maintenance/custom-sidebar-links-section.tsx` | **新文件** — 管理自定义链接的 UI 组件 |
| `web/src/features/system-settings/site/section-registry.tsx` | 注册新的 "自定义侧边栏链接" 设置页面 |
| `web/src/features/system-settings/types.ts` | 在 `SiteSettings` 类型中添加 `CustomSidebarLinks` 字段 |
| `web/src/features/system-settings/site/index.tsx` | 在默认设置中添加 `CustomSidebarLinks` |
| `web/src/features/system-settings/hooks/use-update-option.ts` | 将 `CustomSidebarLinks` 加入 status 相关配置键列表 |
| `web/src/hooks/use-sidebar-data.ts` | 从 status API 读取自定义链接并注入侧边栏 |
| `web/src/components/layout/components/nav-group.tsx` | 修改 `SidebarMenuLink` 支持外部链接渲染 |

---

## 部署方式

### 方式一：使用补丁文件（推荐）

如果你已经 fork 了 `QuantumNous/new-api`：

```bash
# 进入你的 new-api 仓库目录
cd your-new-api-repo

# 应用补丁
git apply custom-sidebar-links.patch

# 提交
git add -A
git commit -m "feat: add custom sidebar links configuration"

# 推送到你的 fork
git push origin main
```

然后重新构建 Docker 镜像部署。

### 方式二：Docker 重新构建

```bash
# 在你的 fork 仓库中
docker build -t new-api-custom-sidebar .

# 替换运行中的容器
docker stop new-api
docker rm new-api
docker run -d --name new-api \
  -p 3000:3000 \
  -v /path/to/data:/data \
  -e SQL_DSN="your-dsn" \
  new-api-custom-sidebar
```

### 方式三：docker-compose

```bash
# 修改 docker-compose.yml 中的 image 为你构建的镜像
docker-compose up -d --build
```

---

## 使用方法

1. 登录管理后台
2. 进入 **系统设置 → 站点**
3. 在左侧子导航中找到 **自定义侧边栏链接**（在"侧边栏模块"下方）
4. 点击 **"Add link"** 添加新链接
5. 填写：
   - **Title**: 显示名称（如 "文档"）
   - **URL**: 链接地址
     - 内部路径：`/pricing`、`/about`
     - 外部链接：`https://docs.example.com`
   - **Icon**: 选择图标（可选）
   - **Badge**: 徽章文字（可选，如 "New"）
   - **Visible to**: 可见角色
     - Everyone (Guest+) — 所有人可见
     - Logged-in users — 仅登录用户
     - Admins only — 仅管理员
     - Super admins only — 仅超级管理员
6. 拖拽链接卡片可以重新排序
7. 点击 **"Save custom links"** 保存

保存后，所有用户的侧边栏会立即显示新的链接（在原有菜单下方，独立的 "Links" 分组中）。

---

## 配置数据格式

配置存储在数据库 `options` 表中，key 为 `CustomSidebarLinks`，value 为 JSON 数组：

```json
[
  {
    "title": "文档",
    "url": "https://docs.example.com",
    "icon": "BookOpen",
    "badge": "",
    "requiredRole": 0
  },
  {
    "title": "API 定价",
    "url": "/pricing",
    "icon": "CreditCard",
    "badge": "",
    "requiredRole": 0
  },
  {
    "title": "管理后台",
    "url": "https://admin.example.com",
    "icon": "Shield",
    "badge": "Staff",
    "requiredRole": 10
  }
]
```

---

## 注意事项

1. **向后兼容**：不配置任何自定义链接时，侧边栏与原版完全一致，不会出现空的 "Links" 分组
2. **角色过滤**：`requiredRole` 为 0 时所有人可见；链接同时受 `SidebarModulesAdmin` 的整体开关影响
3. **安全性**：后端验证 JSON 格式；外部链接使用 `rel="noopener noreferrer"` 防止安全问题
4. **性能**：自定义链接通过 `/api/status` 接口一并返回，无额外网络请求
