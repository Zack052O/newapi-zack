# New-API 自定义侧边栏版 — 使用说明

> 项目地址：https://github.com/Zack052O/newapi-zack
> 基于 QuantumNous/new-api，新增自定义侧边栏链接功能

---

## 一、本地启动

双击工作区的 **启动newapi.command**，或手动执行：

```bash
cd /Volumes/T7/VUE/newapi
./dev.sh
```

- 前端：http://localhost:3050（改代码自动刷新）
- 后端：http://localhost:3000（本地 SQLite，不影响线上）
- 第一次访问 http://localhost:3050/setup 创建管理员账号
- 关闭：双击 **关闭newapi.command**

> 新终端窗口如果提示 `bun: command not found`，先执行 `source ~/.zshrc`

---

## 二、添加自定义侧边栏链接

1. 登录后进入 **系统设置 → 站点 → 自定义侧边栏链接**
2. 点 **Add link**，填写标题、URL、图标
3. 外部链接（`https://...`）自动在新标签打开
4. 支持：图标选择、徽章文字、角色权限、拖拽排序
5. 保存后侧边栏立即出现

---

## 三、改完代码推送到 GitHub

```bash
cd /Volumes/T7/VUE/newapi
git add -A
git commit -m "描述你改了什么"
git push origin main
```

> push 只传代码，不碰任何数据库

---

## 四、部署到服务器（替换原版）

```bash
ssh your-server
cd /path/to/newapi-zack
git pull origin main
docker build -t new-api-custom .

# 重启容器（连接原有 MySQL，数据不变）
docker stop new-api && docker rm new-api
docker run -d --name new-api \
  -p 3000:3000 \
  -v /path/to/data:/data \
  -e SQL_DSN="root:你的密码@tcp(127.0.0.1:3306)/new-api" \
  new-api-custom
```

### SQLite 和 MySQL 的区别

| 环境 | SQL_DSN 的值 | 结果 |
|------|-------------|------|
| 本地开发 | （空，不设） | 自动用 SQLite |
| 线上服务器 | `root:密码@tcp(127.0.0.1:3306)/new-api` | 用 MySQL |

代码不用改，只差一个环境变量。你原版 docker 怎么跑的，新版就怎么跑。

---

## 五、同步上游新功能

```bash
cd /Volumes/T7/VUE/newapi
git fetch upstream
git merge upstream/main
git push origin main
```

也可以在 GitHub 网页点 **Sync fork** 按钮一键同步。

---

## 六、完整流程

```
本地改代码（SQLite 随便测）
    ↓
git push（只传代码）
    ↓
服务器 git pull + docker build（构建新镜像）
    ↓
docker run（连接原有 MySQL，数据不变）
```
